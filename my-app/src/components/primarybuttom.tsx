import React from "react";
import styled from "@emotion/styled";
// import { useState } from "react";
import { Link } from "react-router-dom";

export const primaryButtom = (props: string) => {
    const { children } = prpps;
    return;
    <>
        <Link to="#">{children}</Link>
    </>;
};
